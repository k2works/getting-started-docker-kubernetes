# DB 認証
POSTGRES_USER=cargo
POSTGRES_PASSWORD=cargo-dev-password
POSTGRES_DB=postgres
# JWT（authms / trackingms / billingms / gatewayms）
JWT_SECRET=dev-only-jwt-secret-please-override-in-prod-32bytes
JWT_ISSUER=case-study-cargo-tracker
