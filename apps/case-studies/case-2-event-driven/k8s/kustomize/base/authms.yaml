apiVersion: apps/v1
kind: Deployment
metadata:
  name: authms
  labels:
    app.kubernetes.io/name: authms
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: authms
  template:
    metadata:
      labels:
        app.kubernetes.io/name: authms
    spec:
      containers:
        - name: authms
          image: cargo2-authms:0.0.1
          env:
            - name: PORT
              value: "8081"
            - name: SPRING_PROFILES_ACTIVE
              value: local-prod
            - name: DB_URL
              value: jdbc:postgresql://postgres:5432/auth_db
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: DB_PASSWORD
          ports:
            - name: http
              containerPort: 8081
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 18
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 90
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: authms
  labels:
    app.kubernetes.io/name: authms
spec:
  selector:
    app.kubernetes.io/name: authms
  ports:
    - name: http
      port: 8081
      targetPort: http
